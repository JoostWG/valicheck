name: Publish

permissions:
  id-token: write  # Required for OIDC
  contents: read
on:
  release:
    types:
      - released

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: JoostWG/actions/npm-publish@master
